<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.perswsj.mapper.EmpMapper">
<!--    根据ID更新员工数据 -->
    <update id="updateById">
        UPDATE emp
        <set>
        <if test="username != null and username != ''">username = #{username},</if>
        <if test="password != null and password != ''">password = #{password},</if>
        <if test="name != null and name != ''">name = #{name},</if>
        <if test="gender != null and gender != ''">gender = #{gender},</if>
        <if test="phone != null and phone != ''">phone = #{phone},</if>
        <if test="job != null">job = #{job},</if>
        <if test="salary != null">salary = #{salary},</if>
        <if test="image != null and image != ''">image = #{image},</if>
        <if test="entryDate != null">entry_date = #{entryDate},</if>
        <if test="deptId != null">dept_id = #{deptId},</if>
        update_time = #{updateTime}
        WHERE id = #{id}
        </set>
    </update>
    <delete id="deleteById">
        DELETE FROM emp WHERE id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <select id="list" resultType="org.perswsj.model.Emp">
        SELECT e.*, d.name deptName
        FROM emp e
        LEFT JOIN dept d ON e.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                AND e.name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="gender != null">
                AND e.gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                AND e.entry_date BETWEEN #{begin} AND #{end}
            </if>
        </where>
        ORDER BY e.update_time DESC
    </select>
<!--    自定义结果集 empResultMap，包含员工工作经历-->
    <resultMap id="empResultMap" type="org.perswsj.model.Emp">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="gender" property="gender" />
        <result column="phone" property="phone" />
        <result column="job" property="job" />
        <result column="salary" property="salary" />
        <result column="image" property="image" />
        <result column="entry_date" property="entryDate" />
        <result column="dept_id" property="deptId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="deptName" property="deptName" />

        <collection property="exprList" ofType="org.perswsj.model.EmpExpr">
            <id column="ee_id" property="id" />
            <result column="ee_company" property="company" />
            <result column="ee_job" property="job" />
            <result column="ee_begin" property="begin" />
            <result column="ee_end" property="end" />
            <result column="ee_emp_id" property="empId" />
        </collection>
    </resultMap>
    <select id="getInfo" resultMap="empResultMap">
        SELECT
            e.*,
            ee.id ee_id,
            ee.company ee_company,
            ee.job ee_job,
            ee.begin ee_begin,
            ee.end ee_end,
            ee.emp_id ee_emp_id
        FROM emp e
        LEFT JOIN emp_expr ee ON e.id = ee.emp_id
        WHERE e.id = #{id}
    </select>
    <select id="countEmpJobData" resultType="java.util.Map">
        SELECT
            (CASE WHEN job = 1 THEN '班主任'
            WHEN job = 2 THEN '讲师'
            WHEN job = 3 THEN '学工主管'
            WHEN job = 4 THEN '教研主管'
            WHEN job = 5 THEN '咨询师'
            ELSE '其他' END) AS pos,
        COUNT(*) AS num
        FROM emp
        GROUP BY pos
        ORDER BY num
    </select>
    <select id="countEmpGenderData" resultType="java.util.Map">
        SELECT
           IF(gender = 1, '男性员工', '女性员工') AS name,
        COUNT(*) AS value
        FROM emp
        GROUP BY gender
    </select>
    <select id="countEmpClassData" resultType="java.util.Map">
        SELECT
            c.name AS clazz_name,
            COUNT(s.id) AS value
        FROM clazz c
        LEFT JOIN student s ON c.id = s.clazz_id
        GROUP BY c.name
        ORDER BY value
    </select>
    <select id="countStudentDegreeData" resultType="java.util.Map">
        SELECT
            (CASE WHEN degree = 1 THEN '初中'
            WHEN degree = 2 THEN '高中'
            WHEN degree = 3 THEN '大专'
            WHEN degree = 4 THEN '本科'
            WHEN degree = 5 THEN '硕士'
            WHEN degree = 6 THEN '博士'
            ELSE '其他' END) AS name,
            COUNT(s.id) AS value
        FROM student s
        GROUP BY s.degree
        ORDER BY value
    </select>
    <select id="getEmpCountByDeptId" resultType="java.lang.Integer">
        SELECT
        COUNT(id) AS value
        FROM emp
        WHERE dept_id = #{deptId}
    </select>
</mapper>