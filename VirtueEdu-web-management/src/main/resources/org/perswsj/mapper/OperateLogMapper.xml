<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.perswsj.mapper.OperateLogMapper">
    <select id="list" resultType="org.perswsj.dto.OperateLogDto">
        SELECT
            ol.id,
            ol.operate_emp_id,
            ol.operate_time,
            ol.class_name,
            ol.method_name,
            ol.method_params,
            ol.return_value,
            ol.cost_time,
            e.name as operate_emp_name
        FROM operate_log ol
        LEFT JOIN emp e ON ol.operate_emp_id = e.id
        <where>
            <if test="operateEmpId != null">AND ol.operate_emp_id = #{operateEmpId}</if>
            <if test="beginTime != null">AND ol.operate_time >= #{beginTime}</if>
            <if test="endTime != null">AND ol.operate_time &lt;= #{endTime}</if>
            <if test="className != null and className != ''">AND ol.class_name LIKE CONCAT('%',#{className},'%')</if>
            <if test="methodName != null and methodName != ''">AND ol.method_name LIKE CONCAT('%',#{methodName},'%')</if>
        </where>
        ORDER BY ol.operate_time DESC
    </select>
    <insert id="insert">
        INSERT INTO
            operate_log (operate_emp_id, operate_time, class_name, method_name, method_params, return_value, cost_time)
        VALUES
            (#{operateEmpId}, #{operateTime}, #{className}, #{methodName}, #{methodParams}, #{returnValue}, #{costTime})
    </insert>
</mapper>